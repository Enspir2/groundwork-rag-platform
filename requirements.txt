# Core (Phase 0-1, already usable)
python-dotenv

# AI providers (Phase 2 — embeddings & LLM)
anthropic
voyageai

# Retrieval (Phase 3)
rank-bm25
numpy

# Version pins removed for now — Python 3.14 is very new, and some libraries
# (numpy 1.26.4 specifically) don't have prebuilt wheels for it yet, which
# forces pip to compile from source and fail without a C compiler installed.
# We'll freeze exact working versions once the pipeline is stable
# (via `pip freeze > requirements.txt`) — this is normal practice: pin loosely
# during development, freeze exact versions before "shipping."
