# --- Secrets: NEVER commit these ---
.env
.env.*
!.env.example
*.key
*.pem

# --- Python ---
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/

# --- Data / generated artifacts ---
# revenue.db and embeddings_cache.json are generated, not source — don't track them
data/revenue.db
data/embeddings_cache.json
data/rerank_cache.json
*.log

# --- Java / Spring Boot (for later phases) ---
target/
*.class
.mvn/
!.mvn/wrapper/maven-wrapper.jar

# --- IDE / OS junk ---
.vscode/
.idea/
*.iml
.DS_Store
Thumbs.db

# --- Node (if any tooling needs it later) ---
node_modules/
